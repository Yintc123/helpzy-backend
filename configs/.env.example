# Server
PORT=8080
ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=helpzy

# Redis
REDIS_URL=redis://localhost:6379

# JWT 簽署金鑰（與 frontend BFF 共享相同的值，min 32 chars）
JWT_SECRET=

# CORS（允許的 Origin，填入 Next.js BFF 位址）
ALLOWED_ORIGIN=http://localhost:3000
